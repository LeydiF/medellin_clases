<h1>Students</h1>

<%= link_to "Add student", new_student_path %>

<ul>
	<% @students.each do |student| %>
		<li class="student">
			<span class="student-name"><%= student.name %></span>
			<%= link_to "Open subjects", mostrar_materias_path(student) %>
			<!-- <ul>
				<% student.subjects.each do |subject| %>
					<li>
						<%= subject.name %>
					</li>
				<% end %>
			</ul> -->
		</li>
	<% end %>
</ul>


